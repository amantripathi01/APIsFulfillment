# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=cb128291e8
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "authorizations_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Authorizations API tests"
    flow:
      - log: "Starting test on Authorizations API"
      - get:
          url: "/authorizations/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
      - think: 1
      - log: 
          - "Tested GET request on /authorizations/{{ id }}"
      - delete:
          url: "/authorizations/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
      - think: 1
      - log: 
          - "Tested DELETE request on /authorizations/{{ id }}"

# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user/createWithList_post for http method type POST in artillery framework
# RoostTestHash=70af040573
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_createWithList_post_others.csv"
    fields: ["id", "username", "firstName", "lastName", "email", "password", "phone", "userStatus"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "http://localhost:3000"
      http:
        pool: 10
scenarios:
  - name: "Non-Success API Requests"
    flow:
      - log: "Invalid POST request to /v2/user/createWithList"
      - post:
          url: "/v2/user/createWithList"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            id: "{{$payload.id}}"
            username: "{{$payload.username}}"
            firstName: "{{$payload.firstName}}"
            lastName: "{{$payload.lastName}}"
            email: "{{$payload.email}}"
            password: "{{$payload.password}}"
            phone: "{{$payload.phone}}"
            userStatus: "{{$payload.userStatus}}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals: 
                message: "Invalid input"
      - think: 1
      - log: "Unauthorized POST request to /v2/user/createWithList"
      - post:
          url: "/v2/user/createWithList"
          headers:
            api_key: "INVALID_TOKEN"
            Content-Type: "application/json"
          json:
            id: "{{$payload.id}}"
            username: "{{$payload.username}}"
            firstName: "{{$payload.firstName}}"
            lastName: "{{$payload.lastName}}"
            email: "{{$payload.email}}"
            password: "{{$payload.password}}"
            phone: "{{$payload.phone}}"
            userStatus: "{{$payload.userStatus}}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: 
                message: "Unauthorized"
      - think: 1
      - log: "Non-existent URL"
      - get:
          url: "/invalid/url"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

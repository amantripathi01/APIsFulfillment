# ********RoostGPT********

# Test generated by RoostGPT for test Artillery-test1-unit_clone using AI Type AWS Bedrock Runtime AI and AI Model anthropic.claude-3-sonnet-20240229-v1:0
# 
# Test generated for /products_get for http method type GET in artillery framework
# RoostTestHash=70199667a6
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5

payload:
  fields:
    - "id"
    - "name"
    - "description"
    - "price"
  path: "products_get_success.csv"
  delimiter: "^|^"
  skipHeader: true

scenarios:
  - name: "Successful Product Retrieval"
    flow:
      - log: "Scenario: Successful Product Retrieval"

      - log: "Retrieving all products"
      - get:
          url: "/products"
        expect:
          - statusCode: 200
          - hasProperty:
              property: "$.0.id"
              type: integer
              format: int64
          - hasProperty:
              property: "$.0.name"
              type: string
          - hasProperty:
              property: "$.0.description"
              type: string
          - hasProperty:
              property: "$.0.price"
              type: integer
              format: int64
      - think: 2

      - log: "Verifying individual product details"
      - get:
          url: "/products/{{ id }}"
        headers:
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 200
          - hasProperty:
              property: "id"
              type: integer
              format: int64
          - hasProperty:
              property: "name"
              type: string
          - hasProperty:
              property: "description"
              type: string
          - hasProperty:
              property: "price"
              type: integer
              format: int64
      - think: 2

# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user/logout_get for http method type GET in artillery framework
# RoostTestHash=a64e3feb39
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_logout_get_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Testing non-successful API requests"
    flow:
      - log: "Sending GET request to /v2/user/logout with incorrect URL"
      - get:
          url: "/v3/user/logout"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Sending GET request to /v2/user/logout without authorization token"
      - get:
          url: "/v2/user/logout"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Sending POST request to /v2/user/logout endpoint which only supports GET"
      - post:
          url: "/v2/user/logout"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 405
      - think: 1
      - log: "Sending GET request to /v2/user/logout with invalid headers"
      - get:
          url: "/v2/user/logout"
          headers:
            invalid_header: "invalid_value"
          expect:
            - statusCode: 400
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
      - think: 1
      - log: "Sending GET request to /v2/user/logout expecting XML response"
      - get:
          url: "/v2/user/logout"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
            Accept: "application/xml"
          expect:
            - statusCode: 200
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/xml"}

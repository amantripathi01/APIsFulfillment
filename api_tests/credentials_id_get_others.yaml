# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /credentials/{id}_get for http method type GET in artillery framework
# RoostTestHash=c02d3561ab
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "credentials_id_get_others.csv"
    fields:
      - id
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      variables:
        auth: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Testing GET request with incorrect URL"
      - get:
          url: "/credentials/{{ id }}"
          headers:
            ApiKeyAuth: "{{ auth }}"
          expect:
            - statusCode: 404

      - think: 1

      - log: "Testing GET request with missing authentication"
      - get:
          url: "/credentials/{{ id }}"
          headers:
            ApiKeyAuth: ""
          expect:
            - statusCode: 401

      - think: 1

      - log: "Testing GET request with incorrect id"
      - get:
          url: "/credentials/invalid_id"
          headers:
            ApiKeyAuth: "{{ auth }}"
          expect:
            - statusCode: 400

      - think: 1

      - log: "Testing GET request with non-existing id"
      - get:
          url: "/credentials/1234567890"
          headers:
            ApiKeyAuth: "{{ auth }}"
          expect:
            - statusCode: 404

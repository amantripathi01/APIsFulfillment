# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /store/order/{orderId}_get for http method type GET in artillery framework
# RoostTestHash=3e40e8eac6
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "store_order_orderId_get_others.csv"
    fields:
      - "orderId"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Test non-success API responses"
    flow:
      - log: "Sending GET request with invalid orderId"
      - get:
          url: "/v2/store/order/{{ orderId }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - contentType: "application/json"
          capture:
            - json: "$"
            - header: "Content-Type"
      - think: 1

      - log: "Sending GET request with non-existing orderId"
      - get:
          url: "/v2/store/order/99999"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - contentType: "application/json"
          capture:
            - json: "$"
            - header: "Content-Type"
      - think: 1

      - log: "Sending GET request to an incorrect URL"
      - get:
          url: "/v2/store/wrongUrl/{{ orderId }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

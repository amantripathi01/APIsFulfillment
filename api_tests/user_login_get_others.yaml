# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user/login_get for http method type GET in artillery framework
# RoostTestHash=474b7881ac
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "user_login_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
    delimiter: "^|^"
scenarios:
  - name: "Testing non-success API responses"
    flow:
      - log: "Starting scenario for invalid login"
      - post:
          url: "/v2/user/login?username={{ username }}&password=wrongpassword"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Testing scenario with missing query parameters"
      - get:
          url: "/v2/user/login"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Testing scenario with incorrect URL"
      - get:
          url: "/v2/user/wrongurl"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

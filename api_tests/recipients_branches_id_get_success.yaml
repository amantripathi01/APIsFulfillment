# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /recipients/branches/{id}_get for http method type GET in artillery framework
# RoostTestHash=061355b093
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "recipients_branches_id_get_success.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
  headers:
    ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
  phases:
    - duration: 60
      arrivalRate: 10

scenarios:
  - name: "Successful API Requests"
    flow:
      - log: "Starting test for GET /recipients/branches/{id}"
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
            - hasProperty: "cocBranchNumber"
            - hasProperty: "cocNumber"
            - hasProperty: "createdAt"
            - hasProperty: "deregistrationDate"
            - hasProperty: "eoriNumber"
            - hasProperty: "id"
            - hasProperty: "tradeName"
            - hasProperty: "tradeNames"
            - hasProperty: "updatedAt"
      - think: 1

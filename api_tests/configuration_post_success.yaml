# ********RoostGPT********

# Test generated by RoostGPT for test amanKB using AI Type AWS Bedrock Runtime AI and AI Model anthropic.claude-3-5-sonnet-20240620-v1:0
# 
# Test generated by RoostGPT for test amanKB using AI Type AWS Bedrock Runtime AI and AI Model anthropic.claude-3-5-sonnet-20240620-v1:0
# 
# Test generated for /configuration_post for http method type POST in artillery framework
# RoostTestHash=f72d56dc59
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 1
      arrivalRate: 1
  payload:
    - path: "configuration_post_success.csv"
      fields:
        - "subscriberId"
        - "frequencyBand"
        - "wifiIndex"
        - "passwd"
        - "SSID"
        - "broadcastSSID"
        - "wifiChannel"
      skipHeader: true
      delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: Configuration API Success Scenario
    flow:
      - log: "Starting Configuration API Success Scenario"
      
      - log: "Sending request to enable WiFi"
      - post:
          url: "/configuration"
          headers:
            Content-Type: "application/json"
          json:
            configuration:
              action:
                type: "HGW_WIFI_ENABLE"
                parameters:
                  frequencyBand: "{{ frequencyBand }}"
              customer:
                subscriberId: "{{ subscriberId }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: control
            - hasProperty: configuration
            - hasProperty: "control.type"
            - hasProperty: "control.code"
            - hasProperty: "control.message"
            - hasProperty: "configuration.id"
      - think: 1

      - log: "Sending request to disable WiFi"
      - post:
          url: "/configuration"
          headers:
            Content-Type: "application/json"
          json:
            configuration:
              action:
                type: "HGW_WIFI_DISABLE"
                parameters:
                  frequencyBand: "{{ frequencyBand }}"
              customer:
                subscriberId: "{{ subscriberId }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: control
            - hasProperty: configuration
            - hasProperty: "control.type"
            - hasProperty: "control.code"
            - hasProperty: "control.message"
            - hasProperty: "configuration.id"
      - think: 1

      - log: "Sending request to set WiFi password"
      - post:
          url: "/configuration"
          headers:
            Content-Type: "application/json"
          json:
            configuration:
              action:
                type: "HGW_WIFI_SET_PASSWD"
                parameters:
                  wifiIndex: "{{ wifiIndex }}"
                  passwd: "{{ passwd }}"
                  mode: "WPAand11i"
              customer:
                subscriberId: "{{ subscriberId }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: control
            - hasProperty: configuration
            - hasProperty: "control.type"
            - hasProperty: "control.code"
            - hasProperty: "control.message"
            - hasProperty: "configuration.id"
      - think: 1

      - log: "Sending request for WiFi configuration"
      - post:
          url: "/configuration"
          headers:
            Content-Type: "application/json"
          json:
            configuration:
              action:
                type: "HGW_WIFI_CONFIGURATION"
                parameters:
                  SSID: "{{ SSID }}"
                  broadcastSSID: "{{ broadcastSSID }}"
                  wifiChannel: "{{ wifiChannel }}"
                  wifiIndex: "{{ wifiIndex }}"
                  mode: "WPAand11i"
              customer:
                subscriberId: "{{ subscriberId }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: control
            - hasProperty: configuration
            - hasProperty: "control.type"
            - hasProperty: "control.code"
            - hasProperty: "control.message"
            - hasProperty: "configuration.id"
      - think: 1

      - log: "Sending request for GPON ONT RESET"
      - post:
          url: "/configuration"
          headers:
            Content-Type: "application/json"
          json:
            configuration:
              action:
                type: "GPON_ONT_RESET"
              customer:
                subscriberId: "{{ subscriberId }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: control
            - hasProperty: configuration
            - hasProperty: "control.type"
            - hasProperty: "control.code"
            - hasProperty: "control.message"
            - hasProperty: "configuration.id"
      - think: 1

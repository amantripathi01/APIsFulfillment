# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user/logout_get for http method type GET in artillery framework
# RoostTestHash=a64e3feb39
# 
# 

# ********RoostGPT********
config:
  target: "http://example.com"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "user_logout_get_success.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
    delimiter: "^|^"
scenarios:
  - name: "User logout API test"
    flow:
      - log: "Starting User logout API test"
      - get:
          url: "/v2/user/logout"
          headers:
            api_key: "AUTH_TOKEN"
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - equals: 
                message: "User logged out successfully"
      - think: 5
      - log: "User logout API test completed"

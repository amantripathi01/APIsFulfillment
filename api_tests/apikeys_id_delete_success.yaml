# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /apikeys/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=4affc26394
# 
# 

# ********RoostGPT********
config:
  target: "http://api.example.com" # replace with your actual API host
  plugins:
    expect: {}
  payload:
    path: "apikeys_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "API keys successful operation"
    flow:
      - log: "Starting scenario for successful API operation"
      - get:
          url: "/apikeys/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - equals:
                - "{{ response.headers.api-version }}"
                - "1.0.0"
      - think: 1

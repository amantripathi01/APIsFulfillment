# ********RoostGPT********

# Test generated by RoostGPT for test amanKB using AI Type AWS Bedrock Runtime AI and AI Model anthropic.claude-3-5-sonnet-20240620-v1:0
# 
# Test generated by RoostGPT for test amanKB using AI Type AWS Bedrock Runtime AI and AI Model anthropic.claude-3-5-sonnet-20240620-v1:0
# 
# Test generated for /listener/communicationMessage_post for http method type POST in artillery framework
# RoostTestHash=4e3dac36ed
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 1
      arrivalRate: 1
  payload:
    path: "listener_communicationMessage_post_success.csv"
    skipHeader: true
    delimiter: "^|^"
    fields:
      - "id"
      - "messageType"
      - "company"
      - "openingDate"
      - "closingDate"
      - "emissionDate"
      - "status"
      - "ticketNumber"
      - "type"
      - "eventType"
      - "loss"
      - "networkType"
      - "windowType"
      - "promiseStartDate"
      - "promiseEndDate"
      - "startDate"
      - "endDate"
      - "promiseDate"
      - "editionDate"
      - "editionReason"
      - "cancelDate"
      - "cancelReason"
      - "closureDate"
      - "closureMotive"
      - "downtime"
      - "estimatedDowntime"
      - "reason"
      - "detailedReason"
      - "generalStatus"
      - "affectedStretch"
      - "identifiedFailure"
      - "closureCause"
      - "closureReason"
      - "closureProvidence"
      - "affectedServiceType"
      - "affectedServiceAddressType"
  plugins:
    expect: {}

scenarios:
  - name: "Test successful POST request to /listener/communicationMessage"
    flow:
      - log: "Starting POST request to /listener/communicationMessage"
      - post:
          url: "/listener/communicationMessage"
          json:
            id: "{{ id }}"
            messageType: "{{ messageType }}"
            company: "{{ company }}"
            openingDate: "{{ openingDate }}"
            closingDate: "{{ closingDate }}"
            emissionDate: "{{ emissionDate }}"
            status: "{{ status }}"
            ticketNumber: "{{ ticketNumber }}"
            type: "{{ type }}"
            eventType: "{{ eventType }}"
            loss: "{{ loss }}"
            networkType: "{{ networkType }}"
            windowType: "{{ windowType }}"
            promiseStartDate: "{{ promiseStartDate }}"
            promiseEndDate: "{{ promiseEndDate }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            promiseDate: "{{ promiseDate }}"
            editionDate: "{{ editionDate }}"
            editionReason: "{{ editionReason }}"
            cancelDate: "{{ cancelDate }}"
            cancelReason: "{{ cancelReason }}"
            closureDate: "{{ closureDate }}"
            closureMotive: "{{ closureMotive }}"
            downtime: "{{ downtime }}"
            estimatedDowntime: "{{ estimatedDowntime }}"
            reason: "{{ reason }}"
            detailedReason: "{{ detailedReason }}"
            generalStatus: "{{ generalStatus }}"
            affectedStretch: "{{ affectedStretch }}"
            identifiedFailure: "{{ identifiedFailure }}"
            closureCause: "{{ closureCause }}"
            closureReason: "{{ closureReason }}"
            closureProvidence: "{{ closureProvidence }}"
            affectedService:
              type: "{{ affectedServiceType }}"
              address:
                - type: "{{ affectedServiceAddressType }}"
          expect:
            - statusCode: 204
            - hasHeader: "content-type"
      - think: 1

# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user/{username}_get for http method type GET in artillery framework
# RoostTestHash=da281fe471
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_username_get_others.csv"
    fields:
      - "username"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        pool: 10
scenarios:
  - name: "Non-successful API requests"
    flow:
      - log: "Sending invalid GET request to /v2/user/{username}"
      - get:
          url: "/v2/user/{{ username }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1
      - log: "Sending GET request to non-existing URL"
      - get:
          url: "/v2/non-existing-url"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Sending GET request to /v2/user/{username} without required headers"
      - get:
          url: "/v2/user/{{ username }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1
      - log: "Sending GET request to /v2/user/{username} with empty username"
      - get:
          url: "/v2/user/{}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /apikeys_post for http method type POST in artillery framework
# RoostTestHash=28a54f1579
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "apikeys_post_others.csv"
    fields:
      - "apiKey"
    skipHeader: true
    delimiter: "^|^"
  environments:
    local:
      target: "{{ $processEnvironment.API_HOST }}"
      phases:
        - duration: 60
          arrivalRate: 20
scenarios:
  - name: "Non-Success API Response Test"
    flow:
      - log: "Starting request for invalid API key"
      - post:
          url: "/apikeys?apiKey=invalid"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: "Invalid API Key"
      - think: 1
      - log: "Starting request for missing API key"
      - post:
          url: "/apikeys"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals: "API Key is missing"
      - think: 1
      - log: "Starting request for invalid URL"
      - post:
          url: "/invalidurl"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /recipients/branches_put for http method type PUT in artillery framework
# RoostTestHash=63322ffb26
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "recipients_branches_put_success.csv"
    fields:
      - "cocNumber"
      - "cocBranchNumber"
      - "city"
      - "tradeName"
      - "phone"
      - "email"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Successful API requests test scenario"
    flow:
      - log: "Starting test for POST /recipients/branches endpoint"
      - post:
          url: "/recipients/branches"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasProperty: "cocNumber"
            - hasProperty: "cocBranchNumber"
            - hasProperty: "city"
            - hasProperty: "tradeName"
            - hasProperty: "phone"
            - hasProperty: "email"
            - hasProperty: "id"
            - hasProperty: "eoriNumber"
            - hasProperty: "deregistrationDate"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
      - think: 1

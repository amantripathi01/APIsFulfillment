# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /apikeys_get for http method type GET in artillery framework
# RoostTestHash=9e57a3b0dd
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    - expect
  payload:
    path: "apikeys_get_success.csv"
    fields:
      - "apiKey"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      phases:
        - duration: 60
          arrivalRate: 5
          name: "Warm up the application"
        - duration: 120
          arrivalRate: 5
          rampTo: 50
          name: "Ramp up load"
        - duration: 600
          arrivalRate: 50
          name: "Sustained load"

scenarios:
  - name: "API keys test scenario"
    flow:
      - log: "Starting API keys request"
      - get:
          url: "/apikeys"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "apikeys"
      - think: 1
      - get:
          url: "/apikeys"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - hasProperty: "apikeys[0].id"
            - hasProperty: "apikeys[0].createdAt"
      - think: 1

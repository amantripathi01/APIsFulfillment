# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /store/order/{orderId}_delete for http method type DELETE in artillery framework
# RoostTestHash=6e4c4bbafe
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "store_order_orderId_delete_others.csv"
    fields: ["orderId"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      variables:
        AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Non-Success API Response Checks"
    flow:
      - log: "Performing DELETE request with invalid token"
      - delete:
          url: "/v2/store/order/{{ orderId }}"
          headers:
            api_key: "{{ AUTH_TOKEN }}"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Performing DELETE request on non-existing order ID"
      - delete:
          url: "/v2/store/order/999999999"
          headers:
            api_key: "{{ AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Performing DELETE request with invalid order ID"
      - delete:
          url: "/v2/store/order/invalidID"
          headers:
            api_key: "{{ AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Performing DELETE request on incorrect API endpoint"
      - delete:
          url: "/v2/store/invalidEndpoint/{{ orderId }}"
          headers:
            api_key: "{{ AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

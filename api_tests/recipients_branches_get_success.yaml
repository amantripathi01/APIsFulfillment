# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /recipients/branches_get for http method type GET in artillery framework
# RoostTestHash=74a40da00e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "recipients_branches_get_success.csv"
    fields:
      - "coc-number"
      - "coc-branch-number"
      - "trade-name"
      - "size"
      - "offset"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}
scenarios:
  - name: "Test Scenario - Successful API requests"
    flow:
      - log: "Starting test for /recipients/branches endpoint"
      - get:
          url: "/recipients/branches?coc-number={{ coc-number }}&coc-branch-number={{ coc-branch-number }}&trade-name={{ trade-name }}&size={{ size }}&offset={{ offset }}"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "branches"
            - hasProperty: "total"
            - hasHeader: "api-version"
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
      - think: 1

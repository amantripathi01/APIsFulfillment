# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /store/inventory_get for http method type GET in artillery framework
# RoostTestHash=a66f7079af
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "store_inventory_get_others.csv"
    fields:
      - "invalidApiKey"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Test non-success API responses"
    flow:
      - log: "Sending GET request with invalid API key"
      - get:
          url: "/v2/store/inventory"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending GET request with missing API key"
      - get:
          url: "/v2/store/inventory"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending GET request to non-existent endpoint"
      - get:
          url: "/nonexistent"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

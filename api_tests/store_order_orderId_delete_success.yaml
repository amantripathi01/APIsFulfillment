# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /store/order/{orderId}_delete for http method type DELETE in artillery framework
# RoostTestHash=6e4c4bbafe
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    - expect: {}
  payload:
    path: "store_order_orderId_delete_success.csv"
    fields:
      - "orderId"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: Delete purchase order by ID
    flow:
      - log: "Starting Delete purchase order by ID"
      - delete:
          url: "/v2/store/order/{{ orderId }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "content-type"
            - headerEquals: {"content-type": "application/json"}
      - think: 1

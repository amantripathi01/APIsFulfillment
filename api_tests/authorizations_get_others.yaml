# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /authorizations_get for http method type GET in artillery framework
# RoostTestHash=3b64f22044
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "authorizations_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
    delimiter: "^|^"
  phases:
    - duration: 10
      arrivalRate: 10

scenarios:
  - name: "Non-success API responses test"
    flow:
      - log: "Sending invalid GET request to /authorizations"
      - get:
          url: "/authorizations"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - equals:
                - "error.message"
                - "Unauthorized access"
      - think: 1

      - log: "Sending POST request to /authorizations with invalid payload"
      - post:
          url: "/authorizations"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            username: "{{ username }}"
            password: "{{ invalidpassword }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals:
                - "error.message"
                - "Invalid payload"
      - think: 1

      - log: "Sending DELETE request to /authorizations without authorization"
      - delete:
          url: "/authorizations/{{ username }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - equals:
                - "error.message"
                - "Unauthorized access"
      - think: 1

      - log: "Sending GET request to non-existent endpoint"
      - get:
          url: "/nonexistent"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - equals:
                - "error.message"
                - "Not found"
      - think: 1

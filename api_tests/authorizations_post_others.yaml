# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /authorizations_post for http method type POST in artillery framework
# RoostTestHash=ed8c1b7bff
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "authorizations_post_others.csv"
    fields: ["email"]
    skipHeader: true
    delimiter: "^|^"
  defaults:
    headers:
      ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Non-success API responses"
    flow:
      - log: "Sending invalid payload"
      - post:
          url: "/authorizations"
          headers:
            Content-Type: "application/json"
          json:
            email: "invalid_email"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

      - log: "Sending request without authentication"
      - post:
          url: "/authorizations"
          headers:
            Content-Type: "application/json"
            ApiKeyAuth: ""
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1

      - log: "Sending request to invalid URL"
      - post:
          url: "/invalid_url"
          headers:
            Content-Type: "application/json"
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 404
      - think: 1

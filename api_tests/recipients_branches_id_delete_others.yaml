# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /recipients/branches/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=1ce4bf6e3c
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "recipients_branches_id_delete_others.csv"
    fields: ["id"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "http://dev.api.example.com"
    prod:
      target: "http://api.example.com"
scenarios:
  - name: "Non-success API response tests"
    flow:
      - log: "Making a GET request with invalid id"
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1

      - log: "Making a POST request without payload"
      - post:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

      - log: "Making a DELETE request without authentication"
      - delete:
          url: "/recipients/branches/{{ id }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1

      - log: "Making a PUT request with invalid URL"
      - put:
          url: "/invalid_url/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1

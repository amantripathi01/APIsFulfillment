# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /grants_get for http method type GET in artillery framework
# RoostTestHash=692c6b853f
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "grants_get_others.csv"
    fields:
      - "id"
      - "name"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      headers:
        Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Non-successful API responses"
    flow:
      - log: "Sending an invalid GET request to /grants"
      - get:
          url: "/invalid_url"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending a GET request to /grants with invalid token"
      - get:
          url: "/grants"
          headers:
            ApiKeyAuth: "invalid_token"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending a GET request to /grants with missing token"
      - get:
          url: "/grants"
          headers:
            ApiKeyAuth: ""
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending a GET request to /grants with an invalid id in the payload"
      - get:
          url: "/grants?id=invalid_id"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Sending a GET request to /grants with a missing id in the payload"
      - get:
          url: "/grants?id="
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

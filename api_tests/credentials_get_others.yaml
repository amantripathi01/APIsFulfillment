# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /credentials_get for http method type GET in artillery framework
# RoostTestHash=c5011f3c64
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "credentials_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
    delimiter: "^|^"
scenarios:
  - name: "Test non-success API responses"
    flow:
      - log: "Sending invalid GET request to /credentials"
      - get:
          url: "/invalid/credentials?size={{ size }}&offset={{ offset }}"
          headers:
            Api-Key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1

      - log: "Sending GET request to /credentials without authorization"
      - get:
          url: "/credentials?size={{ size }}&offset={{ offset }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1

      - log: "Sending GET request to /credentials with invalid size"
      - get:
          url: "/credentials?size=invalid&offset={{ offset }}"
          headers:
            Api-Key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

      - log: "Sending GET request to /credentials with invalid offset"
      - get:
          url: "/credentials?size={{ size }}&offset=invalid"
          headers:
            Api-Key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

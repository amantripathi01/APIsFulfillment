# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=cb128291e8
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    default:
      target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "authorizations_id_delete_others.csv"
    fields: ["id"]
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-success API responses"
    flow:
      - log: "Sending DELETE request with invalid ID"
      - delete:
          url: "/authorizations/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
      - think: 1

      - log: "Sending DELETE request without ID"
      - delete:
          url: "/authorizations/"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
      - think: 1

      - log: "Sending DELETE request with incorrect URL"
      - delete:
          url: "/authorize/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
      - think: 1

      - log: "Sending DELETE request without auth token"
      - delete:
          url: "/authorizations/{{ id }}"
          expect:
            - statusCode: 401
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
      - think: 1

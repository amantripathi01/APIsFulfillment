# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user_post for http method type POST in artillery framework
# RoostTestHash=fcaadbca4d
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_post_others.csv"
    fields: ["id", "username", "firstName", "lastName", "email", "password", "phone", "userStatus"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    auth:
      headers:
        api_key: "{{$processEnvironment.AUTH_TOKEN}}"
scenarios:
  - name: "User API failure scenario"
    flow:
      - log: "Attempting to create user with invalid data"
      - post:
          url: "/v2/user"
          headers:
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals: ["message", "Invalid input"]
      - think: 1
      - log: "Attempting to create user without authorization"
      - post:
          url: "/v2/user"
          headers:
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: ["message", "Unauthorized"]
      - think: 1
      - log: "Attempting to create user with invalid URL"
      - post:
          url: "/v3/invalid"
          headers:
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: ["message", "Not Found"]
      - think: 1

# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /apikeys/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=4affc26394
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "apikeys_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}
scenarios:
  - name: Non-Success API Responses
    flow:
      - log: "Sending DELETE request with invalid API Key"
      - delete:
          url: "/apikeys/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 401
          - hasHeader: "content-type"
          - headerEquals: ["content-type", "application/json"]
      - think: 1

      - log: "Sending DELETE request with non-existing ID"
      - delete:
          url: "/apikeys/non-existing-id"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 404
          - hasHeader: "content-type"
          - headerEquals: ["content-type", "application/json"]
      - think: 1

      - log: "Sending DELETE request with malformed ID"
      - delete:
          url: "/apikeys/malformed-id"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 400
          - hasHeader: "content-type"
          - headerEquals: ["content-type", "application/json"]
      - think: 1

# ********RoostGPT********

# Test generated by RoostGPT for test Artillery-test1-unit_clone using AI Type Vertex AI and AI Model claude-3-5-sonnet-v2
# 
# Test generated for /products/{productId}_get for http method type GET in artillery framework
# RoostTestHash=ab8efa24ed
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_get_success.csv"
    skipHeader: true
    delimiter: "^|^"
    fields:
      - "productId"

scenarios:
  - name: "Test successful product retrieval by ID"
    flow:
      - log:
          message: "Starting test to get product by ID"
      
      - get:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - matchesRegexp:
                "id": "^[0-9]+$"
            - matchesRegexp:
                "price": "^[0-9]+$"

      - think:
          duration: 1

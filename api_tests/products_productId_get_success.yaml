# ********RoostGPT********

# Test generated by RoostGPT for test Artillery-test1-unit_clone using AI Type Claude AI and AI Model claude-3-5-sonnet-20240620
# 
# Test generated for /products/{productId}_get for http method type GET in artillery framework
# RoostTestHash=ab8efa24ed
# 
# 

# ********RoostGPT********
config:
  target: "{{$processEnvironment.API_HOST}}"
  phases:
    - duration: 1
      arrivalRate: 1
  payload:
    path: "products_productId_get_success.csv"
    fields:
      - "productId"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: Get product by ID
    flow:
      - log: "Starting scenario: Get product by ID"
      - get:
          url: "/products/{{productId}}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - matchesJsonSchema:
                properties:
                  id:
                    type: "integer"
                  name:
                    type: "string"
                  description:
                    type: "string"
                  price:
                    type: "integer"
                required: ["name"]
          capture:
            - json: "$.id"
              as: "capturedProductId"
            - json: "$.name"
              as: "capturedProductName"
      - log: "Successfully retrieved product with ID: {{capturedProductId}} and name: {{capturedProductName}}"
      - think: 2

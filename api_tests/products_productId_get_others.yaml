# ********RoostGPT********

# Test generated by RoostGPT for test Artillery-test1-unit_clone using AI Type  and AI Model 
# 
# Test generated for /products/{productId}_get for http method type GET in artillery framework
# RoostTestHash=ab8efa24ed
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "products_productId_get_others.csv"
    fields:
      - "productId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Test Product API"
    flow:
      - log: "Starting test for getProductById"
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
      - think: 2

      - log: "Testing with invalid product ID"
      - get:
          url: "/products/invalidId"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Testing with non-existent product ID"
      - get:
          url: "/products/999999"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Testing without authorization"
      - get:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 401
      - think: 1

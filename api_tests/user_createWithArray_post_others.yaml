# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user/createWithArray_post for http method type POST in artillery framework
# RoostTestHash=b50615cd0e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_createWithArray_post_others.csv"
    fields: ["id", "username", "firstName", "lastName", "email", "password", "phone", "userStatus"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "http://localhost:3000"
      http:
        pool: 10
scenarios:
  - name: "Non-Success API Requests"
    flow:
      - log: "Invalid POST request to /v2/user/createWithArray"
      - post:
          url: "/v2/user/createWithArray"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            id: "{{$payload.id}}"
            username: "{{$payload.username}}"
            firstName: "{{$payload.firstName}}"
            lastName: "{{$payload.lastName}}"
            email: "{{$payload.email}}"
            password: "{{$payload.password}}"
            phone: "{{$payload.phone}}"
            userStatus: "{{$payload.userStatus}}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - hasProperty: "error"
            - equals: ["message", "Invalid request"]
      - think: 1
      - log: "Invalid URL"
      - get:
          url: "/v2/user/nonexistent"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - hasProperty: "error"
            - equals: ["message", "URL not found"]
      - think: 1
      - log: "Unauthorized access"
      - post:
          url: "/v2/user/createWithArray"
          headers:
            api_key: "invalid_token"
            Content-Type: "application/json"
          json:
            id: "{{$payload.id}}"
            username: "{{$payload.username}}"
            firstName: "{{$payload.firstName}}"
            lastName: "{{$payload.lastName}}"
            email: "{{$payload.email}}"
            password: "{{$payload.password}}"
            phone: "{{$payload.phone}}"
            userStatus: "{{$payload.userStatus}}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - hasProperty: "error"
            - equals: ["message", "Unauthorized"]
      - think: 1

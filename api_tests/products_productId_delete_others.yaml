# ********RoostGPT********

# Test generated by RoostGPT for test Artillery-test1-unit_clone using AI Type  and AI Model 
# 
# Test generated for /products/{productId}_delete for http method type DELETE in artillery framework
# RoostTestHash=befb82c928
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "products_productId_delete_others.csv"
    fields:
      - "productId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: Delete Product Scenario
    flow:
      - log: "Starting Delete Product request"
      - delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
      - think: 2

      - log: "Attempting to delete non-existent product"
      - delete:
          url: "/products/999999"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Attempting delete with invalid authorization"
      - delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Attempting delete with invalid URL"
      - delete:
          url: "/invalid/url/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

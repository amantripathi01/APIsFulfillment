# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /recipients/branches_get for http method type GET in artillery framework
# RoostTestHash=74a40da00e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "recipients_branches_get_others.csv"
    fields:
      - "coc-number"
      - "coc-branch-number"
      - "trade-name"
      - "size"
      - "offset"
    skipHeader: true
    delimiter: "^|^"
  headers:
    ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Non-success API responses"
    flow:
      - log: "Sending GET request to /recipients/branches with invalid coc-number"
      - get:
          url: "/recipients/branches?coc-number=invalid"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Sending GET request to /recipients/branches with invalid coc-branch-number"
      - get:
          url: "/recipients/branches?coc-branch-number=invalid"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Sending GET request to /recipients/branches with invalid trade-name"
      - get:
          url: "/recipients/branches?trade-name=invalid"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Sending GET request to /recipients/branches with invalid size"
      - get:
          url: "/recipients/branches?size=invalid"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Sending GET request to /recipients/branches with invalid offset"
      - get:
          url: "/recipients/branches?offset=invalid"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Sending GET request to invalid URL"
      - get:
          url: "/invalid-url"
          expect:
            - statusCode: 404
      - think: 1

# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /accounts_patch for http method type PATCH in artillery framework
# RoostTestHash=2758affb7e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "accounts_patch_others.csv"
    fields: ["email"]
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test non-successful API requests"
    flow:
      - log: "Testing with invalid email format"
      - post:
          url: "/accounts"
          json:
            email: "invalid_email"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Testing with missing email field"
      - post:
          url: "/accounts"
          json: {}
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Testing with invalid endpoint"
      - post:
          url: "/invalid_endpoint"
          json:
            email: "{{ email }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Testing with wrong HTTP method"
      - get:
          url: "/accounts"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
      - think: 1

# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /store/order/{orderId}_get for http method type GET in artillery framework
# RoostTestHash=3e40e8eac6
# 
# 

# ********RoostGPT********
config:
  target: "{{ processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "store_order_orderId_get_success.csv"
    fields:
      - "orderId"
    skipHeader: true
  templates:
    headers:
      api_key: "{{ processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Find purchase order by ID"
    flow:
      - log: "Executing GET request to fetch purchase order by ID"
      - get:
          url: "/v2/store/order/{{ orderId }}"
          headers: "{{ headers }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "petId"
            - hasProperty: "quantity"
            - hasProperty: "shipDate"
            - hasProperty: "status"
            - hasProperty: "complete"
      - think: 1

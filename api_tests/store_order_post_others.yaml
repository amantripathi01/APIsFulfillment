# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /store/order_post for http method type POST in artillery framework
# RoostTestHash=d8fbab61da
# 
# 

# ********RoostGPT********
config:
  target: "{{ processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "store_order_post_others.csv"
    fields: ["id", "petId", "quantity", "shipDate", "status", "complete"]
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Starting Scenario: Non-Success API Responses"
      - post:
          url: "/v2/store/order"
          headers:
            api_key: "{{ processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            petId: "{{ petId }}"
            quantity: "{{ quantity }}"
            shipDate: "{{ shipDate }}"
            status: "{{ status }}"
            complete: "{{ complete }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals: ["message", "Invalid Order"]
      - think: 1
      - post:
          url: "/v2/store/order"
          headers:
            api_key: "{{ processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            petId: "{{ petId }}"
            quantity: "{{ quantity }}"
            shipDate: "{{ shipDate }}"
            status: "{{ status }}"
            complete: "{{ complete }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: ["message", "Unauthorized"]
      - think: 1
      - post:
          url: "/v2/store/order"
          headers:
            api_key: "{{ processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            petId: "{{ petId }}"
            quantity: "{{ quantity }}"
            shipDate: "{{ shipDate }}"
            status: "{{ status }}"
            complete: "{{ complete }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: ["message", "Not Found"]
      - think: 1
      - post:
          url: "/invalid_url"
          headers:
            api_key: "{{ processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            petId: "{{ petId }}"
            quantity: "{{ quantity }}"
            shipDate: "{{ shipDate }}"
            status: "{{ status }}"
            complete: "{{ complete }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: ["message", "Not Found"]
      - think: 1

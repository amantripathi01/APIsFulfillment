# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user/login_get for http method type GET in artillery framework
# RoostTestHash=474b7881ac
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_login_get_success.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
    delimiter: "^|^"
  environments:
    production:
      target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
scenarios:
  - name: User login scenario
    flow:
      - log: "Starting User Login Test Scenario"
      - get:
          url: "/v2/user/login?username={{ username }}&password={{ password }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "X-Rate-Limit"
            - hasHeader: "X-Expires-After"
            - hasProperty: "type"
            - equals:
                - "type"
                - "string"
      - log: "User Login Test Scenario Completed"
      - think: 1

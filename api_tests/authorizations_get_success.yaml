# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /authorizations_get for http method type GET in artillery framework
# RoostTestHash=3b64f22044
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "authorizations_get_success.csv"
    fields:
      - "id"
      - "name"
      - "email"
      - "createdAt"
      - "updatedAt"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      headers:
        Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "List accounts who have access to my account"
    flow:
      - log: "Starting scenario: List accounts who have access to my account"
      - get:
          url: "/authorizations"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
      - expect:
          - statusCode: 200
      - expect:
          - hasHeader: "api-version"
      - expect:
          - headerEquals: {"api-version": "1.0.0"}
      - expect:
          - hasProperty: ["authorizations", "total"]
      - expect:
          - equals: ["authorizations[0].id", "{{ id }}"]
      - expect:
          - equals: ["authorizations[0].name", "{{ name }}"]
      - expect:
          - equals: ["authorizations[0].email", "{{ email }}"]
      - expect:
          - equals: ["authorizations[0].createdAt", "{{ createdAt }}"]
      - expect:
          - equals: ["authorizations[0].updatedAt", "{{ updatedAt }}"]
      - think: 1

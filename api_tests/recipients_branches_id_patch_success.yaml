# ********RoostGPT********

# Test generated by RoostGPT for test amanCsvTest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /recipients/branches/{id}_patch for http method type PATCH in artillery framework
# RoostTestHash=9caaa2ef96
# 
# 

# ********RoostGPT********
config:
  target: "{{ processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "recipients_branches_id_patch_success.csv"
    fields:
      - "id"
      - "tradeName"
      - "phone"
      - "email"
    skipHeader: true
    delimiter: "^|^"
  environments:
    staging:
      target: "{{ processEnvironment.API_HOST }}"
      http:
        pool: 10
scenarios:
  - name: "Validate successful API responses"
    flow:
      - log: "Sending PATCH request to /recipients/branches/{id}"
      - patch:
          url: "/recipients/branches/{{ id }}"
          headers:
            Content-Type: "application/json"
            ApiKeyAuth: "{{ processEnvironment.AUTH_TOKEN }}"
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "email"
            - hasProperty: "phone"
            - hasProperty: "cocNumber"
            - hasProperty: "cocBranchNumber"
            - hasProperty: "tradeName"
            - hasProperty: "tradeNames"
            - hasProperty: "eoriNumber"
            - hasProperty: "deregistrationDate"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
      - think: 1

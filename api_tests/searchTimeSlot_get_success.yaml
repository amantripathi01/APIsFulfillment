# ********RoostGPT********

# Test generated by RoostGPT for test amanKB using AI Type AWS Bedrock Runtime AI and AI Model anthropic.claude-3-5-sonnet-20240620-v1:0
# 
# Test generated by RoostGPT for test amanKB using AI Type AWS Bedrock Runtime AI and AI Model anthropic.claude-3-5-sonnet-20240620-v1:0
# 
# Test generated for /searchTimeSlot_get for http method type GET in artillery framework
# RoostTestHash=a43dbdd887
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "searchTimeSlot_get_success.csv"
    skipHeader: true
    delimiter: "^|^"
    fields:
      - "addressId"
      - "subscriberId"
      - "associatedDocument"
      - "startDate"
      - "finishDate"
      - "orderType"
      - "addressChangeFlag"
      - "productType"
      - "priorityFlag"
      - "priorityReason"

scenarios:
  - name: Search Time Slot Scenario
    flow:
      - log: "Starting Search Time Slot test scenario"
      - get:
          url: "/searchTimeSlot?addressId={{ addressId }}&subscriberId={{ subscriberId }}&associatedDocument={{ associatedDocument }}&startDate={{ startDate }}&finishDate={{ finishDate }}&orderType={{ orderType }}&addressChangeFlag={{ addressChangeFlag }}&productType={{ productType }}&priorityFlag={{ priorityFlag }}&priorityReason={{ priorityReason }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: "control"
            - hasProperty: "slots"
            - hasProperty: "control.type"
            - hasProperty: "control.message"
            - hasProperty: "control.code"
            - jmespath: "control.type"
              matches: "^S$"
            - jmespath: "control.code"
              matches: "200"
            - jmespath: "control.message"
              matches: "OK"
            - jmespath: "slots"
              isArray: true
      - think: 1
      - log: "Search Time Slot test scenario completed"

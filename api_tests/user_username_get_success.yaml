# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user/{username}_get for http method type GET in artillery framework
# RoostTestHash=da281fe471
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_username_get_success.csv"
    fields: ["username"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    production:
      target: "{{ $processEnvironment.API_HOST }}"
  headers:
    Content-Type: "application/json"
    api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "Get user by username"
    flow:
      - log: "Starting test scenario for getting user by username"
      - get:
          url: "/v2/user/{{ username }}"
      - capture:
          - header: "Content-Type"
            as: "contentType"
          - json: "$.id"
            as: "id"
          - json: "$.username"
            as: "username"
          - json: "$.firstName"
            as: "firstName"
          - json: "$.lastName"
            as: "lastName"
          - json: "$.email"
            as: "email"
          - json: "$.password"
            as: "password"
          - json: "$.phone"
            as: "phone"
          - json: "$.userStatus"
            as: "userStatus"
      - expect:
          - statusCode: 200
          - contentType: "application/json"
          - hasProperty: "id"
          - hasProperty: "username"
          - hasProperty: "firstName"
          - hasProperty: "lastName"
          - hasProperty: "email"
          - hasProperty: "password"
          - hasProperty: "phone"
          - hasProperty: "userStatus"
      - think: 1

# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /store/inventory_get for http method type GET in artillery framework
# RoostTestHash=a66f7079af
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "store_inventory_get_success.csv"
    fields:
      - "inventoryID"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "API Test Scenario"
    flow:
      - log: "Starting test for /v2/store/inventory"
      - get:
          url: "/v2/store/inventory"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "inventoryID"
      - think: 1

      - log: "Starting test for /v2/store/inventory with specific inventoryID"
      - get:
          url: "/v2/store/inventory?id={{ inventoryID }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "inventoryID"
            - equals: 
                inventoryID: "{{ inventoryID }}"
      - think: 1

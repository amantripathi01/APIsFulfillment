# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user/{username}_delete for http method type DELETE in artillery framework
# RoostTestHash=d8125b8dc9
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_username_delete_others.csv"
    fields:
      - "username"
    skipHeader: true
    delimiter: "^|^"
scenarios:
  - name: "Delete User API failure cases"
    flow:
      - log: "Attempting to delete a user with invalid authorization token"
      - delete:
          url: "/v2/user/{{ username }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Attempting to delete a user that does not exist"
      - delete:
          url: "/v2/user/nonexistentuser"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Attempting to delete a user with missing username"
      - delete:
          url: "/v2/user/"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Attempting to delete a user with incorrect URL"
      - delete:
          url: "/v2/invaliduser/{{ username }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

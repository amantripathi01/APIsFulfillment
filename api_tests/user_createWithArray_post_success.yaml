# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user/createWithArray_post for http method type POST in artillery framework
# RoostTestHash=b50615cd0e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_createWithArray_post_success.csv"
    fields: ["id", "username", "firstName", "lastName", "email", "password", "phone", "userStatus"]
    skipHeader: true
    delimiter: "^|^"
  headers:
    Content-Type: "application/json"
    api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "User creation with array"
    flow:
      - log: "Starting user creation with array test"
      - post:
          url: "/v2/user/createWithArray"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 200
      - hasProperty: "id"
      - hasProperty: "username"
      - hasProperty: "firstName"
      - hasProperty: "lastName"
      - hasProperty: "email"
      - hasProperty: "password"
      - hasProperty: "phone"
      - hasProperty: "userStatus"
      - think: 1

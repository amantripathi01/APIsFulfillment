# ********RoostGPT********

# Test generated by RoostGPT for test amanGRPC using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user/{username}_put for http method type PUT in artillery framework
# RoostTestHash=f6e646c39e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "user_username_put_others.csv"
    fields:
      - "username"
      - "id"
      - "firstName"
      - "lastName"
      - "email"
      - "password"
      - "phone"
      - "userStatus"
    skipHeader: true
    delimiter: "^|^"
scenarios:
  - name: "Negative test scenarios for API requests"
    flow:
      - log: "Starting test for invalid payload"
      - put:
          url: "/v2/user/{{ username }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            username: "{{ username }}INVALID"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Starting test for invalid authentication"
      - put:
          url: "/v2/user/{{ username }}"
          headers:
            api_key: "INVALID_TOKEN"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Starting test for invalid URL"
      - put:
          url: "/v2/userINVALID/{{ username }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 404
      - think: 1
